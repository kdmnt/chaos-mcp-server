{
  "name": "chaos-mcp-server",
  "version": "1.0.0",
  "description": "A chaos MCP server for stress-testing MCP client implementations",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.3.0",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "agents": "^0.5.0",
    "hono": "^4.12.7",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250310.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
